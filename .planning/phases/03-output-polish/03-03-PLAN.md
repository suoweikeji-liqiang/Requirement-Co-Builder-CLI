---
phase: 03-output-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/projects/research.ts
  - src/projects/research.test.ts
  - src/bin/req.ts
  - src/bin/req.test.ts
autonomous: true
requirements:
  - OUT-03
must_haves:
  truths:
    - "User can run `req research add-note <id> <note>` and append a timestamped note entry under the project"
    - "User can run `req research add-link <id> <url> --title <title>` and append a normalized link entry"
    - "Research commands validate project existence and fail with clear messages"
  artifacts:
    - path: "src/projects/research.ts"
      provides: "research note/link persistence service for project directory"
    - path: "src/projects/research.test.ts"
      provides: "tests for note/link append behavior and error paths"
    - path: "src/bin/req.ts"
      provides: "CLI wiring for `req research add-note` and `req research add-link`"
  key_links:
    - from: "src/bin/req.ts"
      to: "src/projects/research.ts"
      via: "research subcommands delegate persistence to research service"
      pattern: "addResearchNote|addResearchLink|research"
    - from: "src/projects/research.ts"
      to: "src/utils/paths.ts"
      via: "service resolves project directory consistently with existing storage modes"
      pattern: "getProjectDir"
---

<objective>
Implement research note/link command workflows.

Purpose: deliver OUT-03 with durable project research logs through explicit CLI commands.
Output: tested research service plus command wiring under `req research`.
</objective>

<execution_context>
@C:/Users/36015/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/36015/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-output-polish/03-RESEARCH.md
@.planning/phases/03-output-polish/03-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add research persistence service</name>
  <files>src/projects/research.ts, src/projects/research.test.ts</files>
  <behavior>
    - appends notes to `research.md` with ISO timestamp and note body
    - appends links to `research.md` with url/title and timestamp
    - validates project existence before write
    - keeps append format readable and stable for future parsing
  </behavior>
  <action>
    Create `src/projects/research.ts` with add-note and add-link service functions.
    Add tests for append order, formatting, and missing-project errors.
  </action>
  <verify>
    <automated>cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/projects/research.test.ts</automated>
  </verify>
  <done>
    Research service supports both note and link append with deterministic output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire req research commands</name>
  <files>src/bin/req.ts, src/bin/req.test.ts</files>
  <behavior>
    - command group `req research` exposes `add-note` and `add-link` subcommands
    - both subcommands honor global `--local`
    - success output confirms append target
  </behavior>
  <action>
    Add `research` command group in `src/bin/req.ts` with subcommand wiring.
    Extend command wiring tests for research commands.
  </action>
  <verify>
    <automated>cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/bin/req.test.ts</automated>
  </verify>
  <done>
    Research commands are available and connected to persistence services.
  </done>
</task>

</tasks>

<verification>
1. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/projects/research.test.ts`
2. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/bin/req.test.ts`
3. `cmd /c npx tsc --noEmit`
</verification>

<success_criteria>
- `req research add-note` and `req research add-link` persist entries in project-local research log.
- Validation and error behavior are consistent with existing project commands.
- Command wiring and service behavior are test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-polish/03-03-SUMMARY.md` following the summary template.
</output>
