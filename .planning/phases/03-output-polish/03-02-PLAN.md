---
phase: 03-output-polish
plan: 02
type: execute
wave: 1
depends_on:
  - 02-02
files_modified:
  - src/output/artifacts.ts
  - src/output/artifacts.test.ts
  - src/dialogue/session.ts
autonomous: true
requirements:
  - OUT-02
  - OUT-04
must_haves:
  truths:
    - "After each persisted chat round, `idea.md` is refreshed with one-liner, three-liner, structured version, stage, and open points"
    - "`decisions.md` receives timestamped entries for explicit confirmation/modification points"
    - "Artifact write failures are surfaced clearly and do not silently corrupt state files"
  artifacts:
    - path: "src/output/artifacts.ts"
      provides: "artifact render and write helpers for idea/decision markdown files"
    - path: "src/output/artifacts.test.ts"
      provides: "deterministic tests for markdown structure and append behavior"
    - path: "src/dialogue/session.ts"
      provides: "chat loop integration for post-round artifact sync"
  key_links:
    - from: "src/dialogue/session.ts"
      to: "src/output/artifacts.ts"
      via: "after state persistence, session writes idea and decision artifacts"
      pattern: "writeIdeaArtifact|appendDecisionEntry|syncRoundArtifacts"
    - from: "src/output/artifacts.ts"
      to: "src/dialogue/engine.ts"
      via: "artifact renderer consumes latest round outputs (compression, assumptions, guard warnings)"
      pattern: "compression|businessAssumptions|guardWarnings"
---

<objective>
Implement automatic project artifact updates from dialogue rounds.

Purpose: satisfy OUT-02 and OUT-04 so each round leaves a durable, human-readable product trace.
Output: tested artifact module and chat-session integration.
</objective>

<execution_context>
@C:/Users/36015/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/36015/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-output-polish/03-RESEARCH.md
@.planning/phases/02-core-dialogue/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add idea and decision artifact writers</name>
  <files>src/output/artifacts.ts, src/output/artifacts.test.ts</files>
  <behavior>
    - writes `idea.md` with fixed sections: one-liner, three-liner, structured, stage, open points
    - appends timestamped entries to `decisions.md` when round content indicates confirmation/modification
    - no-op decision append when no decision signal is present
  </behavior>
  <action>
    Implement render/write helpers in `src/output/artifacts.ts`.
    Add tests for markdown format and decision append rules.
  </action>
  <verify>
    <automated>cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/output/artifacts.test.ts</automated>
  </verify>
  <done>
    Artifact writers generate deterministic markdown and handle append semantics safely.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate artifact sync into chat session</name>
  <files>src/dialogue/session.ts</files>
  <behavior>
    - after `writeProjectState` succeeds, session updates `idea.md`
    - decision log append is attempted each round with deterministic signal detection
    - existing chat output flow remains unchanged for assistant rendering
  </behavior>
  <action>
    Hook artifact writers into `startChatSession` post-persistence path.
    Keep session loop resilient and preserve existing guard/explain output behavior.
  </action>
  <verify>
    <automated>cmd /c npx tsc --noEmit</automated>
  </verify>
  <done>
    Chat rounds continuously maintain project summary and decision log artifacts.
  </done>
</task>

</tasks>

<verification>
1. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/output/artifacts.test.ts`
2. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/dialogue/engine.test.ts`
3. `cmd /c npx tsc --noEmit`
</verification>

<success_criteria>
- `idea.md` is refreshed after every successful round with required fixed structure.
- `decisions.md` captures explicit confirmation/modification turns with timestamps.
- Session loop remains stable and type-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-polish/03-02-SUMMARY.md` following the summary template.
</output>
