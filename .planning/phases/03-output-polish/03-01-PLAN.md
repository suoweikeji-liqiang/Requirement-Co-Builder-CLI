---
phase: 03-output-polish
plan: 01
type: execute
wave: 1
depends_on:
  - 02-04
files_modified:
  - src/output/compile.ts
  - src/output/compile.test.ts
  - src/bin/req.ts
  - src/bin/req.test.ts
autonomous: true
requirements:
  - OUT-01
must_haves:
  truths:
    - "User can run `req build <id>` and receive `spec.md`, `acceptance.md`, and `tasks.md` in the project directory"
    - "Build output is deterministic from current project state and does not require active chat session"
    - "Build command errors clearly when project id is missing or invalid"
  artifacts:
    - path: "src/output/compile.ts"
      provides: "state-to-markdown compile service writing spec/acceptance/tasks files"
    - path: "src/output/compile.test.ts"
      provides: "compile behavior and output file contract tests"
    - path: "src/bin/req.ts"
      provides: "CLI wiring for `req build <id>`"
  key_links:
    - from: "src/bin/req.ts"
      to: "src/output/compile.ts"
      via: "build command delegates to compile service and prints output paths"
      pattern: "buildProjectOutput|compileProjectOutput|build"
    - from: "src/output/compile.ts"
      to: "src/projects/index.ts"
      via: "compile flow loads project state using existing project open/read behavior"
      pattern: "openProject|readProjectState"
---

<objective>
Implement output compilation command for Phase 3.

Purpose: deliver OUT-01 by converting dialogue state into actionable output files for downstream execution.
Output: tested compile service plus `req build <id>` command wiring.
</objective>

<execution_context>
@C:/Users/36015/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/36015/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-output-polish/03-RESEARCH.md
@.planning/phases/02-core-dialogue/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project output compile service</name>
  <files>src/output/compile.ts, src/output/compile.test.ts</files>
  <behavior>
    - compile service resolves project by id and writes `spec.md`, `acceptance.md`, `tasks.md`
    - file content uses current persisted state (idea, stage, projection, latest compression, assumptions/warnings)
    - returns absolute file paths for all generated outputs
    - missing project produces explicit not-found error
  </behavior>
  <action>
    Create compile module with deterministic markdown templates.
    Add tests that verify files are generated and include expected sections.
    Keep compile logic pure and isolated from CLI concerns.
  </action>
  <verify>
    <automated>cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/output/compile.test.ts</automated>
  </verify>
  <done>
    Compile service reliably generates all three output files from project state.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire req build command</name>
  <files>src/bin/req.ts, src/bin/req.test.ts</files>
  <behavior>
    - `req build &lt;id&gt;` triggers compile service using global `--local` option
    - command prints generated file paths
    - command registration test includes `build`
  </behavior>
  <action>
    Add build command to Commander setup in `src/bin/req.ts`.
    Extend CLI wiring tests to assert command registration.
  </action>
  <verify>
    <automated>cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/bin/req.test.ts</automated>
  </verify>
  <done>
    Build command is callable and delegates to compile service.
  </done>
</task>

</tasks>

<verification>
1. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/output/compile.test.ts`
2. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/bin/req.test.ts`
3. `cmd /c npx tsc --noEmit`
</verification>

<success_criteria>
- `req build <id>` writes `spec.md`, `acceptance.md`, and `tasks.md` in project directory.
- Generated output is stable and test-covered.
- CLI wiring follows existing command conventions and `--local` behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-polish/03-01-SUMMARY.md` following the summary template.
</output>
