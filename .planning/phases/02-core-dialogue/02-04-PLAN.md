---
phase: 02-core-dialogue
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/projects/snapshot.ts
  - src/dialogue/explain.ts
  - src/dialogue/session.ts
  - src/bin/req.ts
  - src/projects/snapshot.test.ts
autonomous: true
requirements:
  - PROJ-06
  - SAFE-04
must_haves:
  truths:
    - "User can run `req snapshot <id> --tag <name>` and a timestamped snapshot is created"
    - "Snapshot includes enough project artifacts to preserve dialogue context"
    - "Knowledge explanation content appears only when user triggers `/explain`, `/deep-dive`, or `/later`"
    - "Explanation output is capped to three sentences and includes confidence tagging"
  artifacts:
    - path: "src/projects/snapshot.ts"
      provides: "snapshot creation service and artifact copy/validation logic"
    - path: "src/projects/snapshot.test.ts"
      provides: "snapshot behavior tests"
    - path: "src/dialogue/explain.ts"
      provides: "trigger parsing and explanation formatting policy"
    - path: "src/dialogue/session.ts"
      provides: "integration of explain triggers into chat loop"
    - path: "src/bin/req.ts"
      provides: "snapshot command wiring"
  key_links:
    - from: "src/bin/req.ts"
      to: "src/projects/snapshot.ts"
      via: "snapshot command invokes snapshot service with project id and tag"
      pattern: "snapshotProject|snapshot"
    - from: "src/dialogue/session.ts"
      to: "src/dialogue/explain.ts"
      via: "session loop checks slash commands and routes explanation responses through policy formatter"
      pattern: "/explain|/deep-dive|/later|confidence"
---

<objective>
Implement milestone snapshot command behavior and controlled knowledge explanation triggers in chat sessions.

Purpose: This closes remaining Core Dialogue requirements that are independent from safety policy internals and can run in parallel with plan 02-03.
Output: Snapshot command plus trigger-gated explanation pathway with sentence and confidence constraints.
</objective>

<execution_context>
@C:/Users/36015/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/36015/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-core-dialogue/02-RESEARCH.md
@.planning/phases/02-core-dialogue/02-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add snapshot service and req snapshot command</name>
  <files>src/projects/snapshot.ts, src/projects/snapshot.test.ts, src/bin/req.ts</files>
  <behavior>
    - snapshot service creates `snapshots/<tag-or-timestamp>/` under project directory
    - required artifacts (`state.json`, idea/decision notes if present) are copied into snapshot
    - command validates project existence and reports created snapshot path
    - repeated tags are handled safely (error or unique suffix strategy, but deterministic)
  </behavior>
  <action>
    Implement snapshot logic in `src/projects/snapshot.ts` with clear error messages.
    Add tests in `src/projects/snapshot.test.ts` for successful copy and failure paths.
    Wire `req snapshot <id> --tag <tag>` in `src/bin/req.ts`.
  </action>
  <verify>
    <automated>cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/projects/snapshot.test.ts</automated>
  </verify>
  <done>
    Snapshot command works and produces verifiable snapshot directories with required artifacts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce trigger-only explanation behavior in chat session</name>
  <files>src/dialogue/explain.ts, src/dialogue/session.ts</files>
  <behavior>
    - explanation helper parses slash triggers (`/explain`, `/deep-dive`, `/later`)
    - without trigger, no explanation payload is emitted
    - explanation output is <=3 sentences and includes confidence label
    - session loop integrates helper without breaking normal dialogue turns
  </behavior>
  <action>
    Add explanation policy module in `src/dialogue/explain.ts`.
    Integrate trigger checks and formatted explanation emission inside `src/dialogue/session.ts`.
    Keep this logic isolated from broader safety guard internals for parallel compatibility.
  </action>
  <verify>
    <automated>cmd /c npx tsc --noEmit</automated>
  </verify>
  <done>
    SAFE-04 behavior is enforced with trigger gating, sentence cap, and confidence tagging.
  </done>
</task>

</tasks>

<verification>
1. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/projects/snapshot.test.ts`
2. `cmd /c npx tsc --noEmit`
</verification>

<success_criteria>
- `req snapshot` creates durable, restorable project snapshots.
- SAFE-04 trigger gating works in chat flow with hard sentence cap and confidence labeling.
- Snapshot and explanation behavior are both testable and integrated.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-dialogue/02-04-SUMMARY.md` following the summary template.
</output>
