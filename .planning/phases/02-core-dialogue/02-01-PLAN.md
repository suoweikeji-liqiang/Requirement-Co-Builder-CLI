---
phase: 02-core-dialogue
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/projects/index.ts
  - src/projects/schema.ts
  - src/bin/req.ts
  - src/projects/index.test.ts
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - PROJ-04
must_haves:
  truths:
    - "User can create a project from raw idea text with `req new \"<idea>\"` and receives a project id"
    - "User can list projects with id, clarity stage, and last updated timestamp via `req list`"
    - "User can open a project with `req open <id>` and see current state summary"
    - "User can delete a project with `req delete <id>` and the project directory is removed"
  artifacts:
    - path: "src/projects/index.ts"
      provides: "project lifecycle service: create/list/open/delete"
    - path: "src/projects/schema.ts"
      provides: "project listing and metadata validation types"
    - path: "src/bin/req.ts"
      provides: "command wiring for new/list/open/delete"
    - path: "src/projects/index.test.ts"
      provides: "tests covering lifecycle operations and command-facing behavior"
  key_links:
    - from: "src/bin/req.ts"
      to: "src/projects/index.ts"
      via: "command handlers call create/list/open/delete service functions"
      pattern: "createProject|listProjects|openProject|deleteProject"
    - from: "src/projects/index.ts"
      to: "src/state/index.ts"
      via: "create command uses createInitialState/writeProjectState for persisted state"
      pattern: "createInitialState|writeProjectState|readProjectState"
---

<objective>
Build the project lifecycle command layer for Core Dialogue: create, list, open, and delete project commands.

Purpose: Phase 2 depends on stable project management commands before interactive chat/session logic can be added.
Output: A tested `projects` service module plus CLI command wiring for `req new`, `req list`, `req open`, and `req delete`.
</objective>

<execution_context>
@C:/Users/36015/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/36015/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-core-dialogue/02-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create project service and metadata schema</name>
  <files>src/projects/index.ts, src/projects/schema.ts, src/projects/index.test.ts</files>
  <behavior>
    - createProject(idea, useLocal) creates project directory and state.json, returns project id
    - listProjects(useLocal) returns entries with id, idea, clarityStage, updatedAt sorted by updatedAt desc
    - openProject(projectId, useLocal) returns parsed project state or throws clear not-found error
    - deleteProject(projectId, useLocal) removes the target project directory
    - all functions use existing state/id/path helpers from Phase 1 and do not duplicate persistence logic
  </behavior>
  <action>
    Add `src/projects/schema.ts` for list item typing/validation and `src/projects/index.ts` for project lifecycle operations.
    Use `getProjectsDir`/`getProjectDir`, `generateProjectId`, `createInitialState`, `writeProjectState`, and `readProjectState`.
    Add focused tests in `src/projects/index.test.ts` for create/list/open/delete paths and error cases.
  </action>
  <verify>
    <automated>cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/projects/index.test.ts</automated>
  </verify>
  <done>
    Project lifecycle service exists with passing tests and uses existing Phase 1 state/id abstractions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire req new/list/open/delete commands to project service</name>
  <files>src/bin/req.ts</files>
  <behavior>
    - req new "<idea>" prints project id and created path
    - req list prints a readable table including id, stage, and updated timestamp
    - req open <id> prints project summary (idea, stage, message count, last updated)
    - req delete <id> removes project and confirms deletion
    - use `--local` option for all four commands consistently
  </behavior>
  <action>
    Extend Commander setup in `src/bin/req.ts` with the four commands, importing functions from `src/projects/index.ts`.
    Keep error handling consistent with existing format (print user-friendly error and exit 1).
    Reuse existing global option parsing for `--local`.
  </action>
  <verify>
    <automated>cmd /c npx tsc --noEmit</automated>
  </verify>
  <done>
    CLI exposes and executes all four project lifecycle commands with correct persistence behavior.
  </done>
</task>

</tasks>

<verification>
1. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/projects/index.test.ts`
2. `cmd /c npx tsc --noEmit`
3. `cmd /c npx tsx src/bin/req.ts --help` shows `new`, `list`, `open`, and `delete` commands
</verification>

<success_criteria>
- Project lifecycle commands (`new/list/open/delete`) exist and function end-to-end.
- List output includes clarity stage and updated timestamp.
- Open/delete handle missing project ids with explicit user-facing errors.
- All tests and TypeScript checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-dialogue/02-01-SUMMARY.md` following the summary template.
</output>
