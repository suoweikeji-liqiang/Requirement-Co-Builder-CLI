---
phase: 02-core-dialogue
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/dialogue/guards.ts
  - src/dialogue/logic-chain.ts
  - src/dialogue/engine.ts
  - src/dialogue/guards.test.ts
autonomous: true
requirements:
  - DIAL-05
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-05
must_haves:
  truths:
    - "System flags abstract evaluative claims that lack explicit logic support"
    - "System blocks architecture suggestions before the `structure` stage"
    - "System tracks and flags model-dominant rhythm patterns across rounds"
    - "When causal reasoning appears, output includes a LOGIC_CHAIN block"
    - "When business judgments appear, output includes a BUSINESS_ASSUMPTION block"
  artifacts:
    - path: "src/dialogue/guards.ts"
      provides: "safety guardrail checks for abstract claims, stage gating, and rhythm detection"
    - path: "src/dialogue/logic-chain.ts"
      provides: "LOGIC_CHAIN extraction helpers for CAUSE/EFFECT/ASSUMPTION"
    - path: "src/dialogue/engine.ts"
      provides: "integration point applying guards and optional logic/assumption blocks"
    - path: "src/dialogue/guards.test.ts"
      provides: "guardrail and logic-chain behavioral tests"
  key_links:
    - from: "src/dialogue/engine.ts"
      to: "src/dialogue/guards.ts"
      via: "round output is validated and filtered through policy checks before final render"
      pattern: "enforce|guard|validate"
    - from: "src/dialogue/engine.ts"
      to: "src/dialogue/logic-chain.ts"
      via: "engine attaches LOGIC_CHAIN and BUSINESS_ASSUMPTION blocks when detectors match"
      pattern: "LOGIC_CHAIN|BUSINESS_ASSUMPTION"
---

<objective>
Add deterministic safety guardrails and causal/business reasoning blocks to the dialogue engine.

Purpose: Core dialogue behavior is incomplete without explicit policy enforcement and reasoning transparency requirements.
Output: Guarded dialogue output with LOGIC_CHAIN and BUSINESS_ASSUMPTION support plus tests.
</objective>

<execution_context>
@C:/Users/36015/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/36015/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-core-dialogue/02-RESEARCH.md
@.planning/phases/02-core-dialogue/02-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement safety policy and logic extraction modules</name>
  <files>src/dialogue/guards.ts, src/dialogue/logic-chain.ts, src/dialogue/guards.test.ts</files>
  <behavior>
    - detect unsupported abstract evaluative language and produce warnings/errors
    - detect and block architecture guidance before structure stage
    - detect model-dominant rhythm from recent round history metadata
    - extract LOGIC_CHAIN triples when causal text exists
    - detect business assumptions and produce BUSINESS_ASSUMPTION content
  </behavior>
  <action>
    Implement pure policy/extraction functions in `guards.ts` and `logic-chain.ts`.
    Add unit tests in `guards.test.ts` for all policy scenarios and positive/negative extraction cases.
  </action>
  <verify>
    <automated>cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/dialogue/guards.test.ts</automated>
  </verify>
  <done>
    Safety and reasoning helper modules exist with deterministic tests across all required guardrails.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate safety and reasoning blocks into dialogue engine</name>
  <files>src/dialogue/engine.ts</files>
  <behavior>
    - engine applies safety checks before rendering final output
    - blocked architecture guidance is suppressed or rewritten pre-structure
    - LOGIC_CHAIN block is appended when causal reasoning is detected
    - BUSINESS_ASSUMPTION block is appended when business assumptions are detected
    - rhythm detector state updates per round and influences warning output
  </behavior>
  <action>
    Wire policy and extraction helpers into round orchestration in `engine.ts`.
    Keep integration local to engine so chat command and rendering modules remain stable.
  </action>
  <verify>
    <automated>cmd /c npx tsc --noEmit</automated>
  </verify>
  <done>
    Dialogue output consistently includes required safety behaviors and blocks under matching conditions.
  </done>
</task>

</tasks>

<verification>
1. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/dialogue/guards.test.ts`
2. `cmd /c npx tsc --noEmit`
</verification>

<success_criteria>
- SAFE-01/02/03/05 and DIAL-05 are implemented as deterministic engine behavior.
- LOGIC_CHAIN and BUSINESS_ASSUMPTION blocks appear only when relevant.
- Unsafe pre-structure architecture suggestions are prevented.
- Guardrail behavior is test covered.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-dialogue/02-03-SUMMARY.md` following the summary template.
</output>
