---
phase: 02-core-dialogue
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/dialogue/model.ts
  - src/dialogue/prompts.ts
  - src/dialogue/render.ts
  - src/dialogue/engine.ts
  - src/dialogue/session.ts
  - src/dialogue/engine.test.ts
  - src/bin/req.ts
autonomous: true
requirements:
  - PROJ-05
  - DIAL-01
  - DIAL-02
  - DIAL-03
  - DIAL-04
  - DIAL-06
  - DIAL-07
must_haves:
  truths:
    - "User can resume a saved conversation with `req chat <id>` and continue from persisted context"
    - "Each assistant round restates understanding, advances one point, and asks exactly one question"
    - "Each round persists updated clarity stage and five-dimension projection (Context/Actors/Intent/Mechanism/Boundary)"
    - "Each round outputs LOGIC_BASE plus compressed one-liner, three-liner, and structured rewrite"
  artifacts:
    - path: "src/dialogue/engine.ts"
      provides: "dialogue round orchestration and persistence integration"
    - path: "src/dialogue/model.ts"
      provides: "clarity stage progression and five-dimension model contracts"
    - path: "src/dialogue/render.ts"
      provides: "LOGIC_BASE and compression block rendering"
    - path: "src/dialogue/session.ts"
      provides: "chat session loop and resume behavior"
    - path: "src/bin/req.ts"
      provides: "chat command wiring"
  key_links:
    - from: "src/bin/req.ts"
      to: "src/dialogue/session.ts"
      via: "chat command invokes interactive session loop"
      pattern: "chat|startChatSession"
    - from: "src/dialogue/engine.ts"
      to: "src/adapters/llm.ts"
      via: "engine uses adapter streaming/structured generation for round execution"
      pattern: "createLLMAdapter|generateStructured|streamText"
    - from: "src/dialogue/engine.ts"
      to: "src/state/index.ts"
      via: "each round reads and writes state for persistence and resume"
      pattern: "readProjectState|writeProjectState"
---

<objective>
Implement the core dialogue loop and session resume behavior, including stage progression, five-dimension tracking, LOGIC_BASE, and compressed rewrites.

Purpose: This plan delivers the core interactive value of the product and unlocks follow-on safety and snapshot work.
Output: `req chat <id>` with persisted multi-turn dialogue behavior and structured per-round outputs.
</objective>

<execution_context>
@C:/Users/36015/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/36015/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-core-dialogue/02-RESEARCH.md
@.planning/phases/02-core-dialogue/02-01-PLAN.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build dialogue domain contracts and render blocks</name>
  <files>src/dialogue/model.ts, src/dialogue/prompts.ts, src/dialogue/render.ts, src/dialogue/engine.test.ts</files>
  <behavior>
    - dialogue model defines ClarityStage and five-dimension structure
    - stage progression logic is deterministic and stored with each round
    - renderer produces LOGIC_BASE plus compressed blocks (one-liner, three-liner, structured)
    - helper enforces one-question requirement at render/validation boundary
  </behavior>
  <action>
    Add domain types and helper functions in `model.ts`, prompt builders in `prompts.ts`, and output block builders in `render.ts`.
    Add tests in `engine.test.ts` for stage progression, five-dimension structure, and one-question count checks.
  </action>
  <verify>
    <automated>cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/dialogue/engine.test.ts</automated>
  </verify>
  <done>
    Dialogue contracts and render helpers exist with tests proving stage/model/compression behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement chat engine and req chat command with persistence</name>
  <files>src/dialogue/engine.ts, src/dialogue/session.ts, src/bin/req.ts</files>
  <behavior>
    - `req chat <id>` loads state, enters turn loop, and persists each assistant/user turn
    - engine composes prompts, streams model output, emits LOGIC_BASE + compression blocks, and updates stage/model fields
    - resume continues from prior conversation state without resetting messages or stage
    - chat loop exits cleanly on user command without corrupting state
  </behavior>
  <action>
    Implement `engine.ts` orchestration around existing adapter/state modules.
    Implement interactive loop in `session.ts` using Node readline.
    Wire `chat` command in `req.ts` and keep command-level error handling consistent.
  </action>
  <verify>
    <automated>cmd /c npx tsc --noEmit</automated>
  </verify>
  <done>
    `req chat <id>` is functional, persisted, and resumes correctly with stage/model continuity.
  </done>
</task>

</tasks>

<verification>
1. `cmd /c node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand src/dialogue/engine.test.ts`
2. `cmd /c npx tsc --noEmit`
3. Manual smoke: create project, run `req chat <id>`, exit, run again, confirm prior context resumes
</verification>

<success_criteria>
- `req chat <id>` supports persisted multi-turn dialogue and resume.
- Every round includes LOGIC_BASE and compressed rewrites.
- One-question-per-round and stage progression are enforced.
- Five-dimension model data is persisted in state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-dialogue/02-02-SUMMARY.md` following the summary template.
</output>
